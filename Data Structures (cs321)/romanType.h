class romanType 
{
public:
       int getLarger();
       //function to return the larger of two numerals
       void convertToDecimal();
       //function to convert numerals to decimals
       romanType();
       //constructor to initialize variable to 0
private:
        int numeralOne;
        int numeralTwo;
}
